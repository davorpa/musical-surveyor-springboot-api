/**
 * Controllers are the MVC layer in charge of handle HTTP requests.
 *
 * <p>In a REST application like this:
 * <ol>
 * <li>Uses the web model as Data Transfer Objects (DTOs).
 * <li>Calls the correspondent business service.
 * <li>Sends as a response the domain DTO provided by the service to the end user.
 * </ol>
 */
package io.davorpatech.apps.musicalsurveyor.web.controller;
